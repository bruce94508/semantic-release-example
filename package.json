{
  "name": "semantic-release-example",
  "version": "1.0.0",
  "description": "semantic-release with drone ci use case",
  "devDependencies": {
    "@semantic-release/changelog": "^5.0.1",
    "@semantic-release/git": "^9.0.0"
  }
}
